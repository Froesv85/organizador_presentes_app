specVersion: "v1beta"
serviceId: "organizador_presentes_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "organizador_presentes_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
